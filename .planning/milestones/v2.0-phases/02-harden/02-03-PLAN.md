---
phase: 02-harden
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/app/(marketing)/loading.tsx
  - src/app/(marketing)/blog/loading.tsx
  - src/app/(marketing)/blog/[slug]/loading.tsx
  - src/app/(marketing)/events/loading.tsx
  - src/app/(marketing)/resources/loading.tsx
  - src/app/(marketing)/solutions/loading.tsx
  - src/app/(marketing)/apply/loading.tsx
  - src/app/(marketing)/suggest-event/loading.tsx
autonomous: true
requirements: [FEAT-05]

must_haves:
  truths:
    - "Every route segment shows a shimmer skeleton while loading"
    - "Each loading.tsx mirrors its page's actual layout — blog shows card grid, events shows list, forms show field placeholders"
    - "Shimmer animation uses brand colors with ~2 second sweep cycle"
    - "Navbar and Footer remain visible during loading (skeleton only covers page content)"
  artifacts:
    - path: "src/app/(marketing)/blog/loading.tsx"
      provides: "Blog card grid skeleton"
      contains: "Skeleton"
    - path: "src/app/(marketing)/events/loading.tsx"
      provides: "Events list skeleton"
      contains: "Skeleton"
    - path: "src/app/(marketing)/apply/loading.tsx"
      provides: "Form field skeleton"
      contains: "Skeleton"
    - path: "src/app/(marketing)/loading.tsx"
      provides: "Root marketing loading skeleton"
      contains: "Skeleton"
  key_links:
    - from: "src/app/(marketing)/*/loading.tsx"
      to: "src/components/ui/Skeleton.tsx"
      via: "import { Skeleton } from '@/components/ui/Skeleton'"
      pattern: "import.*Skeleton"
---

<objective>
Create tailored loading.tsx skeletons for all route segments using the Skeleton primitive from Plan 02.

Purpose: Every page shows a premium shimmer loading state that mirrors its actual layout, making the loading experience feel deliberate rather than broken.
Output: 8 loading.tsx files — one per route segment — each composing Skeleton components into page-appropriate layouts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-harden/02-CONTEXT.md
@.planning/phases/02-harden/02-RESEARCH.md
@.planning/phases/02-harden/02-02-SUMMARY.md

<interfaces>
<!-- Skeleton component created in Plan 02 -->
From src/components/ui/Skeleton.tsx:
```typescript
import { cn } from '@/lib/utils'

interface SkeletonProps {
  className?: string
}

export function Skeleton({ className }: SkeletonProps) {
  return <div className={cn('skeleton', className)} />
}
```

<!-- CSS class 'skeleton' provides shimmer animation via globals.css -->
<!-- Usage: <Skeleton className="h-4 w-32" /> for a small text placeholder -->
<!-- Usage: <Skeleton className="h-48 w-full rounded-lg" /> for a card placeholder -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx for content pages (blog, events, resources, solutions)</name>
  <files>
    src/app/(marketing)/loading.tsx,
    src/app/(marketing)/blog/loading.tsx,
    src/app/(marketing)/blog/[slug]/loading.tsx,
    src/app/(marketing)/events/loading.tsx,
    src/app/(marketing)/resources/loading.tsx,
    src/app/(marketing)/solutions/loading.tsx
  </files>
  <action>
Create loading.tsx files for each content page. Do NOT add `'use client'` — loading skeletons are server components (no state, no hooks, pure presentation per research anti-patterns).

Each loading.tsx imports `{ Skeleton }` from `@/components/ui/Skeleton` and composes skeleton shapes to mirror the actual page layout. Before creating each skeleton, read the corresponding `page.tsx` to understand the actual layout structure and mirror it.

**Per-page skeleton designs (locked user decisions — tailored per page):**

1. **`(marketing)/loading.tsx`** — Root marketing fallback. Generic page skeleton: hero area placeholder (large skeleton block), section with a few content blocks below.

2. **`blog/loading.tsx`** — Blog listing page. Mirror the blog page layout:
   - Page hero area: eyebrow skeleton, title skeleton, subtitle skeleton
   - Filter bar skeleton (horizontal row of pill shapes)
   - Card grid: 3-column grid on lg, 1-column on mobile. Each card = image placeholder (aspect-video), title line, date line, tag pill

3. **`blog/[slug]/loading.tsx`** — Single blog post. Mirror article layout:
   - Back link skeleton
   - Post header: title (2 lines), author + date row, category pill
   - Hero image placeholder (full width, aspect-video)
   - Body: several paragraph-width skeleton lines with varying widths (80%, 100%, 60%) to simulate text

4. **`events/loading.tsx`** — Events listing. Mirror events page:
   - Page hero area skeletons
   - Filter bar skeleton
   - List of event items: each row has date block on left, event title + location on right (4-6 rows)

5. **`resources/loading.tsx`** — Resources page. Mirror resource layout:
   - Page hero area skeletons
   - Filter skeleton
   - Grid or list of resource cards (similar to blog cards but adapted to resource layout)

6. **`solutions/loading.tsx`** — Solutions page. Mirror solutions layout:
   - Page hero area skeletons
   - Category filter skeleton
   - Grid of solution cards

**Layout guidelines:**
- Use the same container widths and padding as the actual pages (check page.tsx for `max-w-*`, `px-*` classes)
- Use `gap-*` to match actual spacing
- Skeleton heights should approximate real content: `h-4` for text lines, `h-6` for titles, `h-8` for large titles, `h-48` or `aspect-video` for images
- Vary skeleton line widths (w-1/3, w-2/3, w-full) to look natural, not uniform
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -10 && for f in "src/app/(marketing)/loading.tsx" "src/app/(marketing)/blog/loading.tsx" "src/app/(marketing)/blog/[slug]/loading.tsx" "src/app/(marketing)/events/loading.tsx" "src/app/(marketing)/resources/loading.tsx" "src/app/(marketing)/solutions/loading.tsx"; do test -f "$f" && echo "OK: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <done>6 loading.tsx files exist for content pages, each mirroring the actual page layout with Skeleton components. No 'use client' directive. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx for form pages (apply, suggest-event)</name>
  <files>
    src/app/(marketing)/apply/loading.tsx,
    src/app/(marketing)/suggest-event/loading.tsx
  </files>
  <action>
Create form-specific loading skeletons (locked user decision: form pages get form-specific skeletons with input shapes, label bars, submit button shape).

Before creating each skeleton, read the corresponding page.tsx and form component to understand the form field layout.

1. **`apply/loading.tsx`** — Application form skeleton. This covers chapter, ventures, and solutions sub-routes (they inherit from apply/):
   - Page hero area: eyebrow + title skeletons
   - Form container with form-field skeletons:
     - Label skeleton (h-4 w-24) + input skeleton (h-10 w-full) repeated for 5-6 fields
     - Textarea skeleton (h-24 w-full) for longer fields
     - Submit button skeleton (h-12 w-40) at bottom
   - Mirror the actual form layout spacing and container width

2. **`suggest-event/loading.tsx`** — Event suggestion form skeleton:
   - Similar form-field skeleton pattern as apply
   - Adapted to the suggest-event form's specific field count and layout
   - Read the actual page.tsx/form component to count fields

**Do NOT add `'use client'`** — pure presentation, no hooks.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -10 && test -f "src/app/(marketing)/apply/loading.tsx" && echo "OK: apply/loading.tsx" && test -f "src/app/(marketing)/suggest-event/loading.tsx" && echo "OK: suggest-event/loading.tsx"</automated>
  </verify>
  <done>2 form loading.tsx files exist with form-specific skeletons (input shapes, label bars, submit button shape). No 'use client' directive. TypeScript compiles.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — successful build
3. All 8 loading.tsx files exist
4. Each loading.tsx imports Skeleton from '@/components/ui/Skeleton'
5. None contain 'use client'
6. Content pages have layout-appropriate skeletons (grids, lists, articles)
7. Form pages have form-specific skeletons (inputs, labels, buttons)
</verification>

<success_criteria>
- All 8 route segments have loading.tsx with tailored shimmer skeletons
- Blog shows card grid skeleton, events shows list skeleton, forms show field placeholders
- Skeletons use brand-colored shimmer via Skeleton component
- No 'use client' on any loading.tsx (server components)
- TypeScript and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-harden/02-03-SUMMARY.md`
</output>
