---
phase: 06-event-interactivity
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/(marketing)/events/EventsPageClient.tsx
autonomous: false
requirements: [EVT-01, EVT-02, EVT-03, EVT-04, EVT-05]

must_haves:
  truths:
    - "Register button opens external registration URL in a new tab when registrationLink is populated"
    - "More Info button opens external info URL in a new tab when moreInfoLink is populated"
    - "Read Recap button navigates internally via Next.js Link when recapLink is populated"
    - "Add to Calendar button shows inline dropdown with Google Calendar, Outlook, Apple Calendar, and Download .ics options when event is Confirmed with a date"
    - "No action buttons render when their corresponding data fields are null"
    - "Event images render with object-fit cover when eventImage is populated"
    - "External link buttons show a small diagonal arrow icon"
    - "Notify me when confirmed button is completely removed"
  artifacts:
    - path: "src/app/(marketing)/events/EventsPageClient.tsx"
      provides: "Data-driven action buttons, AddToCalendarDropdown, event image rendering, ExternalActionLink, InternalActionLink"
      contains: "AddToCalendarDropdown"
  key_links:
    - from: "src/app/(marketing)/events/EventsPageClient.tsx"
      to: "src/lib/events.ts"
      via: "imports SAGIEEvent type, buildGoogleCalendarUrl, buildOutlookCalendarUrl"
      pattern: "import.*buildGoogleCalendarUrl.*from.*@/lib/events"
    - from: "src/app/(marketing)/events/EventsPageClient.tsx"
      to: "/api/events/[id]/ics"
      via: "href in AddToCalendarDropdown links"
      pattern: "/api/events/.*/ics"
---

<objective>
Rewire the events page action buttons to use real data, replace status/type gating with field-existence checks, build the inline Add to Calendar dropdown, wire event images, and remove dead buttons.

Purpose: This is the user-facing delivery — users can register, get info, read recaps, add events to their calendar, and see event images. No dead buttons.
Output: Fully interactive events page with data-driven action buttons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-event-interactivity/06-CONTEXT.md
@.planning/phases/06-event-interactivity/06-RESEARCH.md
@.planning/phases/06-event-interactivity/06-01-SUMMARY.md
@src/app/(marketing)/events/EventsPageClient.tsx
@src/lib/events.ts

<interfaces>
<!-- From Plan 01 — SAGIEEvent will have these new fields -->
```typescript
export interface SAGIEEvent {
  // ... existing fields ...
  registrationLink: string | null   // NEW: external registration URL
  moreInfoLink: string | null       // NEW: external info URL
  recapLink: string | null          // NEW: internal blog recap URL
}
```

<!-- From Plan 01 — exported calendar helpers -->
```typescript
export function buildGoogleCalendarUrl(event: SAGIEEvent): string
export function buildOutlookCalendarUrl(event: SAGIEEvent): string
```

<!-- ICS endpoint created in Plan 01 -->
// GET /api/events/[id]/ics — returns .ics file download

<!-- Current ActionLink component to replace -->
```typescript
function ActionLink({ label, href = '#' }: { label: string; href?: string }) {
  return (
    <a href={href} className="font-body uppercase text-label tracking-label text-foreground-muted hover:text-silver hover:-translate-y-px transition-all duration-150 border-b border-border-subtle pb-px">
      {label}
    </a>
  )
}
```

<!-- Current action row to replace (lines 150-177 of EventsPageClient.tsx) -->
<!-- Uses status/type gating — must be replaced with field-existence gating -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire action buttons, image slot, and Add to Calendar dropdown</name>
  <files>src/app/(marketing)/events/EventsPageClient.tsx</files>
  <action>
    Modify EventsPageClient.tsx with the following changes:

    1. ADD IMPORTS at top:
       - import Link from 'next/link'
       - import { buildGoogleCalendarUrl, buildOutlookCalendarUrl } from '@/lib/events'

    2. REPLACE ActionLink component with TWO new components:

       ExternalActionLink({ label, href }: { label: string; href: string }) — renders an anchor with target="_blank" rel="noopener noreferrer". Keep the existing ActionLink className. Add a small external-link SVG icon after the label text. The SVG: a 10x10 viewBox arrow-up-right (path d="M2 8L8 2M3 2h5v5" stroke="currentColor" strokeWidth="1.5" fill="none") with className="inline-block w-2.5 h-2.5 ml-1". Label format: "{label} ->" followed by the icon.

       InternalActionLink({ label, href }: { label: string; href: string }) — renders a Next.js Link component. Same className as current ActionLink. No external icon. Label format: "{label} ->".

    3. CREATE AddToCalendarDropdown component:
       function AddToCalendarDropdown({ event }: { event: SAGIEEvent })
       - Local state: const [open, setOpen] = useState(false)
       - Build URLs: const googleUrl = buildGoogleCalendarUrl(event), const outlookUrl = buildOutlookCalendarUrl(event), const icsUrl = `/api/events/${event.id}/ics`
       - Render a button with the same styling as ActionLink: "+ Add to Calendar"
       - On click: toggle open state
       - Below button: AnimatePresence wrapping a motion.div (when open) with initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: 'auto' }} exit={{ opacity: 0, height: 0 }} transition={{ duration: 0.2, ease: 'easeOut' }} className="overflow-hidden"
       - Inside motion.div: a flex column with 4 links, each styled with className="font-body text-label text-foreground-muted hover:text-silver py-1.5 pl-4 block":
         a. Google Calendar — anchor href={googleUrl} target="_blank" rel="noopener noreferrer"
         b. Outlook — anchor href={outlookUrl} target="_blank" rel="noopener noreferrer"
         c. Apple Calendar — anchor href={icsUrl} download attribute, text "Apple Calendar (.ics)"
         d. Download .ics — anchor href={icsUrl} download attribute, text "Download .ics"

    4. REPLACE the entire action row (the div with className "flex flex-wrap items-center gap-4 mt-2" and its contents, currently lines ~150-177). New action row:

       The div keeps className="flex flex-wrap items-center gap-4 mt-2". Contents:
         - event.registrationLink && ExternalActionLink label="Register" href={event.registrationLink}
         - event.moreInfoLink && ExternalActionLink label="More Info" href={event.moreInfoLink}
         - event.recapLink && InternalActionLink label="Read Recap" href={event.recapLink}
         - event.status === 'Confirmed' && event.date && AddToCalendarDropdown event={event}
         - event.photoGallery && ExternalActionLink label="View Photo Gallery" href={event.photoGallery}
         - event.recordingLink && ExternalActionLink label="Watch Recording" href={event.recordingLink}

       NOTE: Remove ALL status/type gating logic (event.status !== 'Complete', event.type === 'SAGIE Event', etc.). Remove "Notify me when confirmed" entirely. photoGallery and recordingLink are kept with field-existence gating (they already worked this way for Complete events — now they show for any event that has them).

    5. REPLACE the image slot (the div with className "hidden md:flex items-start justify-center rounded bg-background-card border border-border-subtle" and its contents). New image slot:

       Container div with className="hidden md:flex items-start justify-center rounded overflow-hidden bg-background-card border border-border-subtle" and style={{ aspectRatio: '4/3' }}.
       Inside: conditional — if event.eventImage, render img with src={event.eventImage} alt={event.name} className="w-full h-full object-cover" loading="lazy". Otherwise render span with "No image" text (same as current placeholder styling with className="font-body text-foreground-ghost text-label m-auto").

       Note: Use plain img (not next/image) — Notion image URLs are external and would require remotePatterns config. Add loading="lazy" for performance. Add overflow-hidden to the container so object-cover clips correctly.

    6. DELETE the old ActionLink component entirely — it is fully replaced by ExternalActionLink and InternalActionLink.

    7. Verify: Run `npx tsc --noEmit` to confirm type safety. Run `npx next dev` briefly and visually check /events page loads.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Action row uses field-existence gating only — no status/type conditionals except Add to Calendar (Confirmed + date). ExternalActionLink opens new tabs with noopener and external icon. InternalActionLink uses Next.js Link. AddToCalendarDropdown shows inline animated dropdown with 4 calendar options. Image slot renders actual images with object-cover. "Notify me when confirmed" is gone. No dead buttons (no href="#").</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify event interactivity end-to-end</name>
  <files>src/app/(marketing)/events/EventsPageClient.tsx</files>
  <action>
    Human verifies the complete event interactivity feature on the live dev server.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User has confirmed all event action buttons work correctly and no dead buttons exist.</done>
  <what-built>
    Complete event interactivity: data-driven action buttons (Register, More Info, Read Recap), inline Add to Calendar dropdown (Google, Outlook, Apple, .ics download), event image rendering, and removal of all dead/placeholder buttons.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/events
    2. Expand an upcoming event accordion:
       - If event has a registration link in Notion: confirm "Register ->" button appears with external-link icon, opens new tab
       - If event has a more info link: confirm "More Info ->" appears with external-link icon, opens new tab
       - If event has no registration/info link: confirm those buttons do NOT appear
    3. For a Confirmed event with a date: click "+ Add to Calendar" — verify dropdown appears with 4 options (Google Calendar, Outlook, Apple Calendar, Download .ics)
       - Click Google Calendar link — verify it opens Google Calendar with prefilled event details
       - Click Download .ics — verify a .ics file downloads
    4. Expand a past event: confirm photo gallery / recording links appear only when data exists
    5. Confirm "Notify me when confirmed" button is completely gone
    6. If any event has an image in Notion, confirm it renders in the image slot with proper cropping
    7. If any event has NO image, confirm "No image" placeholder text shows
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- `npx vitest run` — all tests pass (including updated events tests from Plan 01)
- Visual: events page loads, action buttons are data-driven, Add to Calendar dropdown works
- No href="#" anywhere in the action row
- No "Notify me when confirmed" button
- External links have target="_blank" rel="noopener noreferrer"
- Read Recap uses Next.js Link (client-side navigation)
</verification>

<success_criteria>
- Users can click Register and land on external registration page in new tab (EVT-01)
- Users can click Add to Calendar and see dropdown with 4 working calendar options (EVT-02)
- Users can click More Info and land on external info page in new tab (EVT-03)
- Users can click Read Recap and navigate to internal blog post (EVT-04)
- No dead buttons appear — every button only renders when its data field is populated (EVT-05)
- Event images render when available, placeholder when not
</success_criteria>

<output>
After completion, create `.planning/phases/06-event-interactivity/06-02-SUMMARY.md`
</output>
