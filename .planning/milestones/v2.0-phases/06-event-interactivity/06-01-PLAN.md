---
phase: 06-event-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/events.ts
  - src/lib/__tests__/events.test.ts
  - src/app/api/events/[id]/ics/route.ts
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03, EVT-04]

must_haves:
  truths:
    - "mapEvent extracts registrationLink, moreInfoLink, and recapLink from Notion URL properties"
    - "mapEvent extracts time from Notion date.start when datetime is present"
    - "GET /api/events/[id]/ics returns a valid .ics file with correct Content-Type and Content-Disposition headers"
    - "ICS content includes event name, date/time, location, description, and registration link"
    - "All-day events use VALUE=DATE format in ICS and date-only format in Google Calendar URLs"
  artifacts:
    - path: "src/lib/events.ts"
      provides: "Extended SAGIEEvent interface with registrationLink, moreInfoLink, recapLink fields and time extraction"
      exports: ["SAGIEEvent", "getUpcomingEvents", "getPastEvents", "buildGoogleCalendarUrl", "buildOutlookCalendarUrl", "buildIcsContent", "escapeIcsText"]
    - path: "src/app/api/events/[id]/ics/route.ts"
      provides: "ICS file download endpoint"
      exports: ["GET"]
    - path: "src/lib/__tests__/events.test.ts"
      provides: "Updated test fixtures and assertions for new fields, plus ICS/calendar URL builder tests"
  key_links:
    - from: "src/app/api/events/[id]/ics/route.ts"
      to: "src/lib/events.ts"
      via: "imports buildIcsContent and mapEvent logic"
      pattern: "import.*from.*@/lib/events"
    - from: "src/lib/__tests__/events.test.ts"
      to: "src/lib/events.ts"
      via: "tests mapEvent new fields and calendar URL builders"
      pattern: "import.*from.*@/lib/events"
---

<objective>
Extend the event data model with three new URL fields (registrationLink, moreInfoLink, recapLink), extract time from Notion date properties, export calendar URL builder functions and ICS content generator, create the /api/events/[id]/ics route handler, and update all tests.

Purpose: Plan 02 (UI) needs the data model fields and calendar helpers to exist before wiring buttons and the Add to Calendar dropdown.
Output: Updated events.ts with new fields + exported helpers, new ICS API route, passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-event-interactivity/06-CONTEXT.md
@.planning/phases/06-event-interactivity/06-RESEARCH.md
@src/lib/events.ts
@src/lib/__tests__/events.test.ts

<interfaces>
<!-- Current SAGIEEvent interface from src/lib/events.ts -->
```typescript
export interface SAGIEEvent {
  id: string
  name: string
  date: string | null
  time: string | null
  type: 'SAGIE Event' | 'Local Event' | 'Webinar' | null
  format: string | null
  status: 'Concept' | 'Planning' | 'Confirmed' | 'Live' | 'Complete' | 'Cancelled'
  venue: string | null
  description: string | null
  expectedAttendees: number | null
  actualAttendees: number | null
  tierTarget: string | null
  chapter: string | null
  speakers: string | null
  webinarLink: string | null
  recordingLink: string | null
  photoGallery: string | null
  eventImage: string | null
}
```

<!-- Current mapEvent from src/lib/events.ts -->
```typescript
function mapEvent(page: any): SAGIEEvent {
  const p = page.properties
  return {
    // ... existing mappings ...
    time: null,  // hardcoded null — needs to extract from Event Date datetime
    webinarLink: p['Webinar Link']?.url ?? null,
    // NEW fields needed: registrationLink, moreInfoLink, recapLink
  }
}
```

<!-- Current test fixture structure from events.test.ts -->
```typescript
const FULL_EVENT_FIXTURE = {
  id: 'event-abc-001',
  properties: {
    'Event Name': { title: [{ plain_text: 'SAGIE Annual Summit' }] },
    'Event Date': { date: { start: '2024-09-20' } },
    // ... needs 'Registration Link', 'More Info Link', 'Recap Link' URL properties
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SAGIEEvent data model and update tests</name>
  <files>src/lib/events.ts, src/lib/__tests__/events.test.ts</files>
  <behavior>
    - Test: mapEvent with full fixture maps registrationLink from 'Registration Link' URL property
    - Test: mapEvent with full fixture maps moreInfoLink from 'More Info Link' URL property
    - Test: mapEvent with full fixture maps recapLink from 'Recap Link' URL property
    - Test: mapEvent with minimal fixture returns null for all three new fields
    - Test: mapEvent extracts time string (HH:mm) when Event Date contains datetime (e.g. '2024-09-20T18:00:00.000+03:00' yields time '18:00')
    - Test: mapEvent returns time null when Event Date is date-only (e.g. '2024-09-20')
    - Test: buildGoogleCalendarUrl produces correct URL with encoded params for timed event
    - Test: buildGoogleCalendarUrl produces date-only format (YYYYMMDD/YYYYMMDD+1) for all-day event
    - Test: buildOutlookCalendarUrl produces correct URL with encoded params
    - Test: buildIcsContent produces valid ICS with VCALENDAR/VEVENT structure, correct DTSTART for timed event
    - Test: buildIcsContent produces VALUE=DATE format for all-day event
    - Test: escapeIcsText escapes commas, semicolons, backslashes, newlines
    - Test: buildIcsContent includes registration link in DESCRIPTION when present
  </behavior>
  <action>
    1. Update FULL_EVENT_FIXTURE in events.test.ts: add 'Registration Link': { url: 'https://example.com/register' }, 'More Info Link': { url: 'https://example.com/info' }, 'Recap Link': { url: '/blog/summit-recap' } properties. Also add a datetime variant fixture where 'Event Date': { date: { start: '2024-09-20T18:00:00.000+03:00' } }.

    2. Write failing tests first for all behaviors listed above. The calendar URL builder and ICS tests should import the new exported functions.

    3. Extend SAGIEEvent interface in events.ts — add three new optional fields:
       - registrationLink: string | null
       - moreInfoLink: string | null
       - recapLink: string | null

    4. Update mapEvent in events.ts:
       - registrationLink: p['Registration Link']?.url ?? null
       - moreInfoLink: p['More Info Link']?.url ?? null
       - recapLink: p['Recap Link']?.url ?? null
       - time: Extract from Event Date datetime. If p['Event Date']?.date?.start contains 'T', parse the time portion (HH:mm). Otherwise null. Use: const dateStr = p['Event Date']?.date?.start ?? null; const time = dateStr?.includes('T') ? dateStr.slice(11, 16) : null

    5. Add exported pure functions to events.ts (at bottom, outside unstable_cache):

       escapeIcsText(text: string): string — escape \, ;, comma, newline per RFC 5545

       buildGoogleCalendarUrl(event: SAGIEEvent): string — construct Google Calendar deep link. For timed events: dates=YYYYMMDDTHHmmss/YYYYMMDDTHHmmss (start + 1 hour). For all-day: dates=YYYYMMDD/YYYYMMDD (next day for end). Include event.description + registration link in details param. Use URLSearchParams for encoding.

       buildOutlookCalendarUrl(event: SAGIEEvent): string — construct Outlook web calendar deep link. Use ISO format for startdt/enddt params. Include subject, body, location.

       buildIcsContent(event: SAGIEEvent): string — generate RFC 5545 ICS string. Use DTSTART;VALUE=DATE for all-day, DTSTART:YYYYMMDDTHHmmssZ for timed. Include UID ({event.id}@sagie.co), DTSTAMP, SUMMARY, DESCRIPTION (with registration link if present), LOCATION. Use \r\n line endings. Filter out empty lines.

    6. Keep webinarLink in the interface for backwards compatibility — do NOT remove it.

    7. Run tests: npx vitest run src/lib/__tests__/events.test.ts
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx vitest run src/lib/__tests__/events.test.ts</automated>
  </verify>
  <done>SAGIEEvent has registrationLink, moreInfoLink, recapLink fields. mapEvent maps all three from Notion URL properties and extracts time from datetime. buildGoogleCalendarUrl, buildOutlookCalendarUrl, buildIcsContent, escapeIcsText are exported and tested. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create ICS API route handler</name>
  <files>src/app/api/events/[id]/ics/route.ts</files>
  <action>
    Create GET route handler at src/app/api/events/[id]/ics/route.ts:

    1. Import { notion } from '@/lib/notion' and { buildIcsContent } from '@/lib/events'. Also import the mapEvent-equivalent logic — since mapEvent is not exported, either: (a) export a mapEventPage helper from events.ts that wraps mapEvent, or (b) inline the Notion page retrieval and use the same property access pattern. Preferred: export mapEvent from events.ts (rename the function export, keep the eslint-disable comment).

    2. Route handler signature — CRITICAL Next.js 16 pattern:
       export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> })
       const { id } = await params

    3. Try/catch the entire handler:
       - Fetch: const page = await notion.pages.retrieve({ page_id: id })
       - Map: const event = mapEvent(page) — using the now-exported function
       - Build: const icsContent = buildIcsContent(event)
       - Slugify filename: event.name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')

    4. Return new Response(icsContent, { headers: { 'Content-Type': 'text/calendar; charset=utf-8', 'Content-Disposition': `attachment; filename="${filename}.ics"`, 'Cache-Control': 'no-store' } })

    5. Catch block: return new Response('Event not found', { status: 404 })

    6. If mapEvent was not already exported in Task 1, go back to events.ts and add the export keyword to mapEvent. Update the eslint-disable comment to stay on the export line.

    7. Verify the build compiles: npx next build (or at minimum npx tsc --noEmit to type-check)
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx vitest run src/lib/__tests__/events.test.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GET /api/events/[id]/ics route exists, uses async params (Next.js 16), fetches event from Notion, returns .ics file with proper headers, returns 404 on error. Type-checks cleanly.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/events.test.ts` — all tests pass including new field mapping and calendar helper tests
- `npx tsc --noEmit` — no type errors
- SAGIEEvent interface includes registrationLink, moreInfoLink, recapLink
- buildGoogleCalendarUrl, buildOutlookCalendarUrl, buildIcsContent are exported from events.ts
- /api/events/[id]/ics/route.ts exists with GET export
</verification>

<success_criteria>
- All new and existing events.test.ts tests pass
- ICS builder produces valid RFC 5545 output for both timed and all-day events
- Google Calendar URL uses correct compact datetime format
- API route type-checks with Next.js 16 async params pattern
- No regressions in existing event data mapping
</success_criteria>

<output>
After completion, create `.planning/phases/06-event-interactivity/06-01-SUMMARY.md`
</output>
