---
phase: 01-stabilize
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GlobeNetwork.tsx
  - src/constants/blog.ts
  - src/constants/events.ts
  - src/constants/resources.ts
  - src/constants/solutions.ts
  - package.json
  - package-lock.json
  - src/app/api/applications/membership/route.ts
autonomous: true
requirements: [BUG-02, CLN-01, CLN-02, CLN-03]

must_haves:
  truths:
    - "Globe component unmounts without a runaway setTimeout loop"
    - "No MOCK_POSTS, MOCK_EVENTS, MOCK_RESOURCES, or MOCK_PROVIDERS constants exist in codebase"
    - "@typeform/embed-react and dotenv are absent from package.json"
    - "No duplicate SolutionProvider or BlogPost interfaces exist in constants files"
  artifacts:
    - path: "src/components/GlobeNetwork.tsx"
      provides: "Globe with cancelledRef cleanup and retry limit"
      contains: "cancelledRef"
    - path: "src/constants/blog.ts"
      provides: "Blog constants without MOCK_POSTS or BlogPost interface"
      contains: "BLOG_CATEGORIES"
    - path: "src/constants/solutions.ts"
      provides: "Solutions constants without MOCK_PROVIDERS or SolutionProvider interface"
      contains: "SERVICE_CATEGORIES"
  key_links:
    - from: "src/components/GlobeNetwork.tsx"
      to: "cancelledRef"
      via: "useRef cleanup in useEffect return"
      pattern: "cancelledRef\\.current = true"
    - from: "src/constants/blog.ts"
      to: "src/components/ui/BlogFilter.tsx"
      via: "BLOG_CATEGORIES and BLOG_AUTHORS exports still consumed"
      pattern: "BLOG_CATEGORIES|BLOG_AUTHORS"
---

<objective>
Fix the Globe setTimeout memory leak, delete all mock data constants, remove unused npm packages, and delete duplicate type interfaces from constants files.

Purpose: The globe leaks setTimeout calls on unmount. Four MOCK arrays and two unused packages are dead weight. Two duplicate interfaces in constants files will confuse future work.
Output: Clean codebase with no memory leaks, no dead code, no unused dependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md

@src/components/GlobeNetwork.tsx
@src/constants/blog.ts
@src/constants/events.ts
@src/constants/resources.ts
@src/constants/solutions.ts

<interfaces>
<!-- Executor needs to know which exports to KEEP vs DELETE in constants files -->

From src/constants/blog.ts — KEEP these (imported by BlogFilter.tsx):
```typescript
export const BLOG_CATEGORIES = [...]   // keep
export const BLOG_AUTHORS = [...]      // keep
```
DELETE: `BlogPost` interface (lines 1-14), `MOCK_POSTS` array

From src/constants/solutions.ts — KEEP these (imported by solutions/page.tsx, SolutionsFilter.tsx):
```typescript
export type SolutionCategory = ...     // keep
export const SERVICE_CATEGORIES = [...] // keep
export const FILTER_OPTIONS = [...]    // keep
```
DELETE: `SolutionProvider` interface (lines 1-10), `MOCK_PROVIDERS` array

From src/constants/events.ts — DELETE entire file (zero external imports)
From src/constants/resources.ts — DELETE entire file (zero external imports)

From src/components/GlobeNetwork.tsx (lines 114-144):
```typescript
// Current broken pattern — no cleanup, no retry limit
const initGlobe = () => {
  if (!globeRef.current) {
    setTimeout(initGlobe, 100)  // runaway loop on unmount
    return
  }
  // ... controls/camera setup
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Globe setTimeout cleanup and delete mock data + duplicate types</name>
  <files>src/components/GlobeNetwork.tsx, src/constants/blog.ts, src/constants/events.ts, src/constants/resources.ts, src/constants/solutions.ts</files>
  <action>
**src/components/GlobeNetwork.tsx (BUG-02):**

1. Add a `cancelledRef` at component top level (alongside existing `globeRef`, `containerRef`):
   ```typescript
   const cancelledRef = useRef(false)
   ```

2. Add a cleanup-only useEffect (can go right after the existing resize useEffect around line 112):
   ```typescript
   useEffect(() => {
     cancelledRef.current = false
     return () => {
       cancelledRef.current = true
     }
   }, [])
   ```

3. Replace the `initGlobe` function definition (lines 114-144) with a version that checks `cancelledRef` and has a max retry counter:
   ```typescript
   const initGlobe = (retries = 0) => {
     if (cancelledRef.current) return
     if (!globeRef.current) {
       if (retries >= 50) return  // max 5 seconds (50 x 100ms)
       setTimeout(() => initGlobe(retries + 1), 100)
       return
     }
     // ... rest of the function body stays exactly the same (controls, camera, pointOfView, maxDistance timeout)
   }
   ```
   Keep all existing code inside initGlobe after the guard clause unchanged — the controls setup, autoRotate, change listener, pointOfView call, and the maxDistance setTimeout at the end.

**src/constants/events.ts (CLN-01):**
Delete the entire file. Zero external imports confirmed by grep.

**src/constants/resources.ts (CLN-01):**
Delete the entire file. Zero external imports confirmed by grep.

**src/constants/blog.ts (CLN-01 + CLN-03):**
- Delete the `BlogPost` interface (lines 1-14). All real consumers import from `@/lib/blog`.
- Delete the `MOCK_POSTS` array (the exported array of mock blog posts).
- KEEP `BLOG_CATEGORIES` and `BLOG_AUTHORS` — these are actively imported by `BlogFilter.tsx`.
- The file should contain only the `BLOG_CATEGORIES` and `BLOG_AUTHORS` exports after editing.

**src/constants/solutions.ts (CLN-01 + CLN-03):**
- Delete the `SolutionProvider` interface (lines 1-10). All real consumers import from `@/lib/solutions`.
- Delete the `MOCK_PROVIDERS` array.
- KEEP `SolutionCategory` type, `SERVICE_CATEGORIES`, and `FILTER_OPTIONS` — these are actively imported.
- The file should contain only those three exports after editing.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit && ! grep -r "MOCK_POSTS\|MOCK_EVENTS\|MOCK_RESOURCES\|MOCK_PROVIDERS" src/ --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>Globe has cancelledRef cleanup and 50-retry limit. No MOCK_* constants in codebase. No duplicate BlogPost or SolutionProvider interfaces in constants files. BLOG_CATEGORIES, BLOG_AUTHORS, SERVICE_CATEGORIES, FILTER_OPTIONS, SolutionCategory still exported and used. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall unused npm packages</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm uninstall @typeform/embed-react dotenv` from the project root.

After the command completes, verify:
1. `@typeform/embed-react` is absent from `package.json` dependencies and devDependencies
2. `dotenv` is absent from `package.json` dependencies and devDependencies
3. Both are absent from `package-lock.json` as top-level entries (they may remain as transitive deps of other packages — that is fine)

If `npm uninstall` doesn't clean the lockfile properly (rare), run `npm install` afterward to regenerate it.

Do NOT run `npm audit fix` or any other npm command that modifies dependencies beyond the two being removed.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; if(deps['@typeform/embed-react']||deps['dotenv']){console.error('FAIL: unused packages still present');process.exit(1)} else {console.log('PASS: packages removed')}"</automated>
  </verify>
  <done>@typeform/embed-react and dotenv are absent from package.json. Lockfile is consistent.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes
2. `npx next lint` passes
3. `npx next build` succeeds
4. `grep -r "MOCK_POSTS\|MOCK_EVENTS\|MOCK_RESOURCES\|MOCK_PROVIDERS" src/` returns no results
5. `@typeform/embed-react` and `dotenv` absent from `package.json`
6. No `SolutionProvider` interface in `src/constants/solutions.ts`
7. No `BlogPost` interface in `src/constants/blog.ts`
</verification>

<success_criteria>
- Globe initGlobe has cancelledRef guard and stops retrying after 50 attempts
- Zero MOCK_* constants anywhere in src/
- @typeform/embed-react and dotenv removed from package.json
- Duplicate SolutionProvider and BlogPost interfaces removed from constants files
- Kept exports (BLOG_CATEGORIES, BLOG_AUTHORS, SERVICE_CATEGORIES, FILTER_OPTIONS, SolutionCategory) still work
- `npx tsc --noEmit` and `npx next build` pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-02-SUMMARY.md`
</output>
