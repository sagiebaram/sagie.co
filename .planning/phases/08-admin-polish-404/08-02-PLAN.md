---
phase: 08-admin-polish-404
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/not-found.tsx
autonomous: false
requirements: [POL-03]

must_haves:
  truths:
    - "Visitor who navigates to a non-existent URL sees a circuit-board SVG illustration with traces leading to a broken/disconnected node"
    - "The broken node gently pulses via CSS animation — no JavaScript required"
    - "The 404 page displays the heading '404' at text-stat (96px) size"
    - "The page shows 'This page doesn't exist, but there's plenty to explore.' copy"
    - "Go Home and Contact Us action buttons are present and functional"
    - "Animation respects prefers-reduced-motion"
  artifacts:
    - path: "src/app/not-found.tsx"
      provides: "Custom 404 page with circuit-board broken-path SVG illustration"
      contains: "broken-node"
  key_links:
    - from: "src/app/not-found.tsx"
      to: "src/components/ui/GridBackground.tsx"
      via: "import GridBackground"
      pattern: "GridBackground"
    - from: "src/app/not-found.tsx"
      to: "src/app/globals.css"
      via: "pulse-node CSS animation on .broken-node class"
      pattern: "broken-node"
---

<objective>
Replace the generic compass-style SVG illustration on the 404 page with a branded circuit-board SVG showing traces leading to a broken/disconnected node, with a subtle CSS pulse animation.

Purpose: The current 404 illustration is generic and doesn't match SAGIE's tech/network aesthetic. A circuit-board broken-path design communicates "path doesn't lead anywhere" while reinforcing brand identity (POL-03).

Output: Branded 404 page with animated circuit-board illustration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-polish-404/08-CONTEXT.md
@.planning/phases/08-admin-polish-404/08-RESEARCH.md
@.planning/phases/08-admin-polish-404/08-01-SUMMARY.md

@src/app/not-found.tsx
@src/components/ui/GridBackground.tsx
@src/components/ui/Button.tsx

<interfaces>
<!-- Current not-found.tsx structure to preserve -->
From src/app/not-found.tsx:
```typescript
// Server component — NO 'use client'. Must stay server component.
// Uses: GridBackground, Button (both from @/components/ui/)
// Heading: font-display text-silver text-stat mb-4 tracking-heading → "404" at 96px
// Copy: "This page doesn't exist, but there's plenty to explore."
// Buttons: Button variant="outline" href="/" + Button variant="ghost" href="mailto:contact@sagie.co"
```

From src/components/ui/Button.tsx:
```typescript
// Button renders <a> tag — works for href-based navigation (Go Home, Contact Us)
export function Button({ variant, children, className, href, ...rest }: ButtonProps)
```

From src/app/globals.css (added by Plan 01):
```css
@keyframes pulse-node {
  0%, 100% { opacity: 0.3; }
  50% { opacity: 0.8; }
}

@media (prefers-reduced-motion: reduce) {
  .broken-node { animation: none; opacity: 0.5; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 404 illustration with circuit-board broken-path SVG</name>
  <files>src/app/not-found.tsx</files>
  <action>
Replace the `NotFoundIllustration` function in `src/app/not-found.tsx` with a new `CircuitBrokenIllustration` function. Keep everything else in the file identical — same layout, same heading (text-stat "404"), same copy, same buttons, same GridBackground import.

**CRITICAL: Do NOT add 'use client' to this file.** It must remain a server component. CSS animations work without JavaScript.

**Replace the NotFoundIllustration function with CircuitBrokenIllustration:**

Design a circuit-board SVG (~120x100 viewBox) with these elements:
- Multiple trace lines (horizontal and vertical) connecting junction nodes in a circuit-board pattern
- A clear visual "break" in one trace path — dashed line segment or gap before the disconnected node
- The broken/disconnected node: a larger circle with dashed stroke, given `className="broken-node"` for the CSS pulse animation
- 3-4 connected junction nodes (small solid circles with low fill opacity) creating the network feel
- All strokes in `#C0C0C0` (silver) with varied `strokeOpacity` (0.3-0.6) for depth
- The broken node should be visually prominent — larger radius, positioned as the "destination" that was never reached

**Apply the pulse animation using an inline style tag** (server component safe):
```tsx
<style>{`
  .broken-node {
    animation: pulse-node 2.5s ease-in-out infinite;
  }
`}</style>
```

Note: The `@keyframes pulse-node` and `@media (prefers-reduced-motion)` rules are already in globals.css (added by Plan 01). This inline style only needs the class-to-animation binding.

**Add `aria-label="Circuit board with disconnected node"` to the SVG element** for accessibility.

**Keep the wrapper div unchanged:** `<div className="mb-8 opacity-60" aria-hidden="true">` — the outer div is already aria-hidden, but the SVG aria-label helps screen reader users who navigate by element.

The rest of not-found.tsx (imports, NotFound component, layout structure, heading, copy, buttons) must remain exactly as-is. Only the illustration function changes.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- NotFoundIllustration replaced with CircuitBrokenIllustration showing circuit-board traces and broken node
- Broken node has className="broken-node" with CSS pulse animation via inline style tag
- SVG has aria-label for accessibility
- not-found.tsx remains a server component (no 'use client')
- Heading stays at text-stat (96px), copy and buttons unchanged
- Animation respects prefers-reduced-motion via globals.css media query
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of 404 illustration</name>
  <files>src/app/not-found.tsx</files>
  <action>
Human verifies the 404 page visual quality. No code changes — this is a visual checkpoint.

What was built: Custom 404 page with circuit-board broken-path SVG illustration and pulsing disconnected node animation.

How to verify:
1. Run `npx next dev` if not already running
2. Navigate to http://localhost:3000/this-page-does-not-exist
3. Verify: Circuit-board SVG illustration with traces and a broken/disconnected node is visible
4. Verify: The broken node gently pulses (opacity fading in and out)
5. Verify: "404" heading is large (96px), silver colored
6. Verify: "This page doesn't exist, but there's plenty to explore." copy is present
7. Verify: "Go home" and "Contact us" buttons are present and clickable
8. Verify: GridBackground pattern is visible behind the content
9. Verify: Overall feel matches the site's dark tech aesthetic

Resume signal: Type "approved" or describe any visual adjustments needed.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user approval"</automated>
  </verify>
  <done>User confirms 404 page visual quality matches SAGIE brand aesthetic</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `npx vitest run` passes (no test changes needed)
3. Visual verification: /nonexistent-page shows branded circuit-board 404
4. Animation runs smoothly, respects prefers-reduced-motion
</verification>

<success_criteria>
- 404 page renders circuit-board SVG with broken node illustration
- Pulse animation runs via CSS only (no JavaScript, no 'use client')
- Page layout, heading size, copy, and buttons unchanged from current
- Build succeeds, existing tests pass
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-polish-404/08-02-SUMMARY.md`
</output>
