---
phase: 03-features-globe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/email.ts
  - src/emails/ConfirmationEmail.tsx
  - src/emails/AdminAlertEmail.tsx
  - src/env/server.ts
  - src/app/api/applications/membership/route.ts
  - src/app/api/applications/chapter/route.ts
  - src/app/api/applications/ventures/route.ts
  - src/app/api/applications/solutions/route.ts
  - src/app/api/submit-post/route.ts
  - src/app/api/submit-resource/route.ts
  - src/app/api/suggest-event/route.ts
autonomous: true
requirements: [FEAT-02, FEAT-03]
user_setup:
  - service: resend
    why: "Email delivery for form confirmations and admin alerts"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify sagie.co domain"
        location: "Resend Dashboard -> Domains -> Add Domain -> sagie.co"

must_haves:
  truths:
    - "An applicant who submits any form with an email field receives a confirmation email"
    - "The admin team receives an alert email with full submission details for every form submission"
    - "Email failures are captured in Sentry, not thrown to the user"
    - "Email sending is skipped in non-production environments"
  artifacts:
    - path: "src/lib/email.ts"
      provides: "sendEmails() helper wrapping Resend with Sentry capture and NODE_ENV gate"
      exports: ["sendEmails", "FormType"]
    - path: "src/emails/ConfirmationEmail.tsx"
      provides: "React Email template for applicant confirmation"
      exports: ["ConfirmationEmail"]
    - path: "src/emails/AdminAlertEmail.tsx"
      provides: "React Email template for admin alert with submission details"
      exports: ["AdminAlertEmail"]
  key_links:
    - from: "src/app/api/applications/membership/route.ts"
      to: "src/lib/email.ts"
      via: "void sendEmails() after notionWrite"
      pattern: "void sendEmails\\("
    - from: "src/lib/email.ts"
      to: "resend"
      via: "resend.emails.send()"
      pattern: "resend\\.emails\\.send"
    - from: "src/lib/email.ts"
      to: "@sentry/nextjs"
      via: "Sentry.captureException on send failure"
      pattern: "Sentry\\.captureException"
---

<objective>
Install Resend + React Email, create email helper and templates, and hook email sending into all 7 API routes.

Purpose: Applicants get confirmation that their submission was received; the admin team gets alerted with full details for every submission without needing to check Notion.
Output: Email infrastructure (helper + 2 templates) and all 7 API routes updated to fire-and-forget emails after successful Notion writes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-features-globe/03-CONTEXT.md
@.planning/phases/03-features-globe/03-RESEARCH.md

@src/env/server.ts
@src/lib/notion-monitor.ts
@src/lib/schemas.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From src/lib/notion-monitor.ts:
```typescript
export async function notionWrite<T>(fn: () => Promise<T>): Promise<T>
// Wraps Notion calls with Sentry.captureException on failure
```

From src/env/server.ts:
```typescript
const EnvSchema = z.object({
  NOTION_TOKEN: z.string().min(1),
  // ... other env vars
  REVALIDATE_SECRET: z.string().min(1).optional(),
  NODE_ENV: z.enum(['development', 'test', 'production']),
})
export const env = EnvSchema.parse(process.env)
```

From src/lib/schemas.ts (email fields per form):
- MembershipSchema: body.email (z.string().email())
- ChapterSchema: body.email (z.string().email())
- VenturesSchema: body.email (z.string().email())
- SolutionsSchema: body.email (z.string().email())
- SubmitPostSchema: body.yourEmail (z.string().email())
- EventSuggestionSchema: NO EMAIL FIELD (suggestedBy is a name string)
- ResourceSchema (inline in submit-resource/route.ts): NO EMAIL FIELD (name + url only)

All 7 routes follow pattern:
```typescript
export const POST = withValidation(Schema, async (_req: Request, body) => {
  try {
    await notionWrite(() => notion.pages.create({...}))
    return NextResponse.json({ success: true })
  } catch (error) { ... }
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add env var, create email helper and templates</name>
  <files>
    package.json
    src/env/server.ts
    src/lib/email.ts
    src/emails/ConfirmationEmail.tsx
    src/emails/AdminAlertEmail.tsx
  </files>
  <action>
1. Install: `npm install resend @react-email/components`

2. Add RESEND_API_KEY to src/env/server.ts EnvSchema as `RESEND_API_KEY: z.string().min(1).optional()` — optional because non-production environments skip sending entirely (consistent with REVALIDATE_SECRET pattern from Phase 1).

3. Create src/lib/email.ts:
   - Import Resend, Sentry, env, both email templates
   - Instantiate `const resend = new Resend(env.RESEND_API_KEY)` (uses env validated value, not raw process.env)
   - Export type `FormType` union: 'Membership Application' | 'Chapter Lead Application' | 'Ventures Intake' | 'Solutions Provider' | 'Event Suggestion' | 'Blog Post Submission' | 'Resource Submission'
   - Export async function `sendEmails(formType: FormType, applicantEmail: string | null, submissionData: Record<string, unknown>): Promise<void>`
   - Gate: if `env.NODE_ENV !== 'production'`, console.log `[email] skip (non-production): ${formType}` and return
   - Subject pattern: `SAGIE \u2014 ${formType} Received`
   - Use Promise.allSettled with two sends:
     a. Confirmation to applicantEmail (SKIP if applicantEmail is null — EventSuggestion and Resource forms have no email)
     b. Admin alert to 'hello@sagie.co' with subject `[ADMIN] ${subject}`
   - Each .catch wraps with `Sentry.captureException(err, { tags: { service: 'resend', type: 'confirmation' | 'admin_alert' } })`
   - From address: `SAGIE <hello@sagie.co>`
   - Use `react:` param for both sends (Resend handles HTML rendering)

4. Create src/emails/ConfirmationEmail.tsx:
   - Import Html, Body, Container, Heading, Text, Img, Hr, Link, Section from @react-email/components
   - Props: `{ formType: string }`
   - Light theme (white background #ffffff, dark text) for universal email client rendering
   - SAGIE logo: `<Img src="https://sagie.co/logo-white.png" alt="SAGIE" width="120" />` (NOTE: logo-white.png on white bg may need logo-black.png instead — use logo-black.png for light-themed email)
   - Heading: "Thank you for your {formType}!"
   - Body text: "We've received your application and our team will review it shortly."
   - Hr separator
   - Footer: "Shape a Great Impact Everywhere" tagline in small text
   - Social links: LinkedIn (https://www.linkedin.com/company/sagie-co) and Instagram (https://www.instagram.com/sagie.co/)
   - All inline styles (email client requirement)

5. Create src/emails/AdminAlertEmail.tsx:
   - Props: `{ formType: string; data: Record<string, unknown> }`
   - Light theme matching ConfirmationEmail
   - Heading: "[ADMIN] New {formType}"
   - Render ALL submission data as key-value pairs in a table/list — admin can triage without opening Notion
   - For each entry in data: display key (capitalized, spaces from camelCase) and value
   - Skip keys with null/undefined/empty string values
   - Same footer as ConfirmationEmail (logo, tagline, social links)
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - resend and @react-email/components in package.json dependencies
    - RESEND_API_KEY optional in env schema
    - src/lib/email.ts exports sendEmails and FormType
    - Both email templates render valid React Email components
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook sendEmails into all 7 API routes</name>
  <files>
    src/app/api/applications/membership/route.ts
    src/app/api/applications/chapter/route.ts
    src/app/api/applications/ventures/route.ts
    src/app/api/applications/solutions/route.ts
    src/app/api/submit-post/route.ts
    src/app/api/submit-resource/route.ts
    src/app/api/suggest-event/route.ts
  </files>
  <action>
In each of the 7 API route files, add `import { sendEmails } from '@/lib/email'` and insert a fire-and-forget `void sendEmails(...)` call AFTER the successful `notionWrite()` and BEFORE the `return NextResponse.json({ success: true })`.

The `void` keyword ensures the API response returns immediately without awaiting email delivery.

Per-route specifics (formType, email source):

1. **membership/route.ts**: `void sendEmails('Membership Application', body.email, body)`
2. **chapter/route.ts**: `void sendEmails('Chapter Lead Application', body.email, body)`
3. **ventures/route.ts**: `void sendEmails('Ventures Intake', body.email, body)`
4. **solutions/route.ts**: `void sendEmails('Solutions Provider', body.email, body)`
5. **submit-post/route.ts**: `void sendEmails('Blog Post Submission', body.yourEmail, body)` (note: field is `yourEmail` not `email`)
6. **suggest-event/route.ts**: `void sendEmails('Event Suggestion', null, body)` (no email field in EventSuggestionSchema — pass null, sendEmails skips confirmation)
7. **submit-resource/route.ts**: `void sendEmails('Resource Submission', null, body)` (no email field in ResourceSchema — pass null, sendEmails skips confirmation)

Do NOT modify any other logic in the routes. Each route should only gain 2 new lines: the import and the void sendEmails call.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -30 && grep -l "sendEmails" src/app/api/*/route.ts src/app/api/applications/*/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    - All 7 API route files import and call sendEmails
    - Routes with email fields pass applicant email; routes without pass null
    - Fire-and-forget pattern (void keyword) used in all routes
    - TypeScript compiles without errors
    - grep confirms sendEmails present in exactly 7 route files
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "sendEmails" src/app/api/` shows 7 route files
3. `grep "RESEND_API_KEY" src/env/server.ts` confirms env var added
4. All email template files exist and export named components
</verification>

<success_criteria>
- resend and @react-email/components installed
- RESEND_API_KEY in env schema (optional)
- sendEmails helper handles NODE_ENV gate, Sentry capture, null email
- Both email templates use React Email components with light theme
- All 7 API routes call sendEmails fire-and-forget after notionWrite
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-features-globe/03-01-SUMMARY.md`
</output>
