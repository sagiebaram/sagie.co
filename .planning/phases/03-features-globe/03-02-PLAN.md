---
phase: 03-features-globe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/data/ne_110m_admin_0_countries.geojson
  - src/env/server.ts
  - src/lib/members.ts
  - src/lib/chapters.ts
  - src/components/GlobeNetwork.tsx
  - src/components/GlobeShell.tsx
autonomous: true
requirements: [GLOBE-01, GLOBE-02]
user_setup:
  - service: notion
    why: "Chapters database for globe chapter data"
    env_vars:
      - name: NOTION_CHAPTERS_DB_ID
        source: "Notion -> Chapters database -> Share -> Copy link -> extract database ID from URL"
    dashboard_config:
      - task: "Create Chapters database with fields: Name (title), Status (select: Active/Coming Soon/Planned), Location (text), Chapter Lead (text), Contact (email/URL), Description (rich text), Founded (date), Agreement (file/URL)"
        location: "Notion workspace"
      - task: "Update Members DB Location field values to canonical city names (Miami, Dallas, Tel Aviv, Singapore, Dubai, New York, London, etc.)"
        location: "Notion -> Members database"

must_haves:
  truths:
    - "The globe loads country polygons from /public/data/ instead of fetching from GitHub"
    - "The globe shows real city markers sourced from Notion member data, not MOCK_CITIES"
    - "Chapter cities have star markers and pulse rings; non-chapter cities are plain dots"
    - "Cities with more members appear larger/brighter (heat glow)"
  artifacts:
    - path: "public/data/ne_110m_admin_0_countries.geojson"
      provides: "Bundled country polygon GeoJSON"
    - path: "src/lib/members.ts"
      provides: "getMemberCities() — cached Notion query returning CityData[]"
      exports: ["getMemberCities", "CityData"]
    - path: "src/lib/chapters.ts"
      provides: "getChapters() — cached Notion query returning Chapter[]"
      exports: ["getChapters", "Chapter"]
    - path: "src/components/GlobeNetwork.tsx"
      provides: "Globe component accepting cities prop instead of using MOCK_CITIES"
  key_links:
    - from: "src/components/GlobeNetwork.tsx"
      to: "/data/ne_110m_admin_0_countries.geojson"
      via: "fetch('/data/ne_110m_admin_0_countries.geojson')"
      pattern: "fetch\\('/data/"
    - from: "src/lib/members.ts"
      to: "notion.databases.query"
      via: "unstable_cache wrapper querying NOTION_MEMBER_DB_ID"
      pattern: "unstable_cache"
    - from: "src/components/GlobeShell.tsx"
      to: "src/lib/members.ts"
      via: "Server component calling getMemberCities() and passing as prop"
      pattern: "getMemberCities"
---

<objective>
Bundle GeoJSON locally, create Notion data-fetching libs for members and chapters, and refactor GlobeNetwork to display real member data from Notion.

Purpose: The globe becomes a live visualization of SAGIE's global community, showing real member locations and chapter status instead of hardcoded mock data.
Output: Static GeoJSON in /public/data/, two new lib files (members.ts, chapters.ts), and a refactored GlobeNetwork that accepts city data as props.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-features-globe/03-CONTEXT.md
@.planning/phases/03-features-globe/03-RESEARCH.md

@src/components/GlobeNetwork.tsx
@src/components/GlobeShell.tsx
@src/lib/blog.ts
@src/env/server.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From src/lib/blog.ts (canonical unstable_cache pattern):
```typescript
export const getAllPosts = unstable_cache(
  async (): Promise<BlogPost[]> => {
    const response = await notion.databases.query({
      database_id: env.NOTION_BLOG_DB_ID,
      filter: { property: 'Status', select: { equals: 'Published' } },
      sorts: [{ property: 'Publish Date', direction: 'descending' }],
    })
    return response.results.map((page: any) => { ... })
  },
  ['notion:blog:index'],
  { revalidate: 3600, tags: ['notion:blog'] }
)
```

From src/components/GlobeNetwork.tsx (current City interface):
```typescript
interface City {
  id: string
  name: string
  lat: number
  lng: number
  members: number
  isChapter: boolean
}
```

From src/components/GlobeShell.tsx (current usage):
```typescript
export function GlobeShell() {
  return (
    <div className="relative aspect-square w-full max-w-[420px]">
      <Suspense fallback={...}>
        <GlobeNetwork />
      </Suspense>
    </div>
  )
}
```

GlobeShell is imported in: src/components/sections/SocialProof.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle GeoJSON, add env var, create members.ts and chapters.ts</name>
  <files>
    public/data/ne_110m_admin_0_countries.geojson
    src/env/server.ts
    src/lib/members.ts
    src/lib/chapters.ts
  </files>
  <action>
1. Download GeoJSON to public/data/:
   ```bash
   mkdir -p public/data
   curl -o public/data/ne_110m_admin_0_countries.geojson \
     "https://raw.githubusercontent.com/vasturiano/react-globe.gl/master/example/datasets/ne_110m_admin_0_countries.geojson"
   ```
   File is ~450KB of country polygon data. Verify it's valid JSON after download.

2. Add NOTION_CHAPTERS_DB_ID to src/env/server.ts EnvSchema as `NOTION_CHAPTERS_DB_ID: z.string().min(1).optional()` — optional because the user needs to create the Chapters database and set the env var. When absent, getChapters() returns empty array (all cities appear as non-chapters).

   NOTE: Plan 03-01 also modifies server.ts (adds RESEND_API_KEY). Since both plans are Wave 1, the executor should read the file fresh before editing. Both add optional fields — no conflict.

3. Create src/lib/members.ts:
   - Export interface `CityData { id: string; name: string; lat: number; lng: number; members: number; isChapter: boolean }`
   - Define static `CITY_COORDS: Record<string, { lat: number; lng: number }>` lookup table with these cities:
     Miami (25.7617, -80.1918), New York (40.7128, -74.006), Dallas (32.7767, -96.797),
     Tel Aviv (32.0853, 34.7818), Singapore (1.3521, 103.8198), Dubai (25.2048, 55.2708),
     London (51.5074, -0.1278), Los Angeles (34.0522, -118.2437), San Francisco (37.7749, -122.4194),
     Chicago (41.8781, -87.6298), Austin (30.2672, -97.7431), Boston (42.3601, -71.0589)
   - Export `getMemberCities` using `unstable_cache` pattern:
     - Import notion from '@/lib/notion', env from '@/env/server'
     - Query `env.NOTION_MEMBER_DB_ID` with filter `{ property: 'Status', select: { does_not_equal: 'Rejected' } }` (include New, Active, Pending Review members)
     - Group results by Location property: `(page as any).properties['Location']?.select?.name`
     - Use case-insensitive matching against CITY_COORDS keys (normalize both to lowercase for comparison)
     - For each city found in CITY_COORDS, build CityData with member count
     - Skip cities not in CITY_COORDS (they have no coordinates to display)
     - isChapter defaults to false here — cross-referencing with chapters happens in the calling code
     - Cache key: `['notion:members:cities']`, revalidate: 3600, tags: `['notion:members']`
   - Export `CITY_COORDS` so chapters.ts can reference it

4. Create src/lib/chapters.ts:
   - Export interface `Chapter { id: string; name: string; status: 'Active' | 'Coming Soon' | 'Planned'; location: string; chapterLead: string | null; description: string | null }`
   - Export `getChapters` using `unstable_cache` pattern:
     - If `env.NOTION_CHAPTERS_DB_ID` is undefined, return empty array (graceful degradation)
     - Query Chapters DB, no filter (return all chapters regardless of status)
     - Map properties: Name (title), Status (select), Location (text), Chapter Lead (text), Description (rich text)
     - Cache key: `['notion:chapters:index']`, revalidate: 3600, tags: `['notion:chapters']`
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && test -f public/data/ne_110m_admin_0_countries.geojson && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - GeoJSON file exists in public/data/ and is valid JSON
    - NOTION_CHAPTERS_DB_ID optional in env schema
    - members.ts exports CityData, getMemberCities, CITY_COORDS
    - chapters.ts exports Chapter, getChapters with graceful fallback when DB ID missing
    - Both use unstable_cache pattern matching existing blog.ts/events.ts
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor GlobeNetwork to accept props and use bundled GeoJSON</name>
  <files>
    src/components/GlobeNetwork.tsx
    src/components/GlobeShell.tsx
  </files>
  <action>
1. Refactor src/components/GlobeNetwork.tsx:

   a. Change the component signature to accept props: `export function GlobeNetwork({ cities }: { cities: CityData[] })`
      - Import CityData type from '@/lib/members' (type-only import: `import type { CityData } from '@/lib/members'`)
      - Remove the local `City` interface (replaced by CityData)
      - Remove `MOCK_CITIES` and `MOCK_ARCS` constants entirely

   b. Replace the GeoJSON fetch URL:
      - Change `fetch('https://raw.githubusercontent.com/vasturiano/react-globe.gl/master/example/datasets/ne_110m_admin_0_countries.geojson')`
      - To `fetch('/data/ne_110m_admin_0_countries.geojson')`

   c. Replace all references to `MOCK_CITIES` with the `cities` prop:
      - `pointsData={[...cities]}`
      - `htmlElementsData={[...cities]}`
      - In onPointClick: `const city = cities.find(c => c.id === point.id)`
      - In htmlElement onclick: `const city = cities.find(c => c.id === d.id) || null`
      - In legend section: `{cities.map((city) => (...))}`

   d. Generate arcs dynamically from chapter cities only (per user decision — arcs between chapter cities):
      - Compute arcs with `useMemo`: filter cities where `isChapter === true`, generate pairs between all chapter cities
      - If fewer than 2 chapter cities, arcs array is empty (no arcs shown)
      - Replace `MOCK_ARCS` references with computed arcs
      - Update arcColor callback to use `cities` not `MOCK_CITIES`

   e. Update ringsData useMemo to use `cities` prop instead of MOCK_CITIES:
      - `cities.filter((c) => c.isChapter).map(...)` — keep existing ring config

   f. Implement heat glow via scaled pointRadius and pointAltitude:
      - pointRadius: `(d: any) => { const base = d.isChapter ? 0.6 : 0.3; const scale = Math.log10(Math.max(d.members, 1) + 1) * 0.4; return base + scale; }`
      - pointAltitude: `(d: any) => 0.02 + (d.members / 200) * 0.04` (max ~0.06 for 200+ members)
      - These replace the current static pointAltitude={0.02} and conditional pointRadius

   g. Update ringsData maxR to scale with member count: `maxR: 14 + Math.log10(Math.max(c.members, 1) + 1) * 6` (larger rings for bigger chapters)

   h. Update the `Arc` interface to match the dynamically-generated shape or remove it if arcs are generated inline.

2. Refactor src/components/GlobeShell.tsx to become a Server Component that fetches data:

   a. Remove `'use client'` directive — this becomes a Server Component
   b. Import `getMemberCities` from '@/lib/members' and `getChapters` from '@/lib/chapters'
   c. Make it async: `export async function GlobeShell()`
   d. Fetch data: `const [memberCities, chapters] = await Promise.all([getMemberCities(), getChapters()])`
   e. Cross-reference: mark cities as chapters by checking if their name matches a chapter's location field (case-insensitive)
   f. Pass merged data to GlobeNetwork: `<GlobeNetwork cities={mergedCities} />`
   g. Keep the dynamic import of GlobeNetwork with `ssr: false` — but move it to a new small client wrapper or keep the dynamic import pattern. Since GlobeShell is now a Server Component, the dynamic import with `ssr: false` still works (Next.js supports this). The Suspense wrapper stays.

   IMPORTANT: The `dynamic()` import with `ssr: false` requires `'use client'`. Since GlobeShell becomes a server component, create a thin client wrapper:
   - Keep GlobeShell as async Server Component that fetches data
   - Create or inline a client-side `GlobeClient` component that uses `dynamic(() => import('./GlobeNetwork'), { ssr: false })` and accepts + passes through the cities prop
   - GlobeShell renders `<GlobeClient cities={mergedCities} />` inside Suspense

   Pattern:
   ```
   GlobeShell (server, fetches data) -> GlobeClient (client, dynamic import) -> GlobeNetwork (client, renders globe)
   ```

   Add GlobeClient.tsx to files_modified if creating a new file. Keep it minimal — just the dynamic import wrapper that passes cities prop through.
  </action>
  <verify>
    <automated>cd /Users/sagie_root/S.A.G.I.E/sagie.co && npx tsc --noEmit 2>&1 | head -20 && grep -c "MOCK_CITIES\|MOCK_ARCS" src/components/GlobeNetwork.tsx</automated>
  </verify>
  <done>
    - GlobeNetwork accepts cities prop, no MOCK_CITIES or MOCK_ARCS remain (grep count = 0)
    - GeoJSON fetched from /data/ path, not GitHub
    - GlobeShell is a Server Component that calls getMemberCities() and getChapters()
    - Chapter cities show star markers + pulse rings, non-chapters are dots
    - Heat glow: pointRadius and pointAltitude scale with member count
    - Arcs generated dynamically from chapter cities only
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -c "MOCK_CITIES\|MOCK_ARCS" src/components/GlobeNetwork.tsx` returns 0
3. `grep "fetch('/data/" src/components/GlobeNetwork.tsx` confirms local GeoJSON path
4. `grep "getMemberCities\|getChapters" src/components/GlobeShell.tsx` confirms data fetching
5. `test -f public/data/ne_110m_admin_0_countries.geojson` confirms bundled file
</verification>

<success_criteria>
- GeoJSON bundled locally in /public/data/
- members.ts and chapters.ts follow established unstable_cache pattern
- MOCK_CITIES and MOCK_ARCS completely removed from GlobeNetwork
- Globe receives real data as props from Server Component
- Chapter distinction preserved (stars + rings vs dots)
- Heat glow scales with member count
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-features-globe/03-02-SUMMARY.md`
</output>
